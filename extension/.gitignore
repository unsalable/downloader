# The private half of the extension signing key, written by
# scripts/extension-key.mjs. Whoever holds it can publish an update that Chrome
# accepts as this extension.
key.pem

# Output of `npm pack`-style zipping, if anyone does it in place.
*.zip
